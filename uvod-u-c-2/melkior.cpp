#include <iostream>
#include <cstdio>
#include <string>

using namespace std;

int main() {
  float a, s, d, f, g;
  cin >> a >> s >> d >> f >> g;
  float ukupno = (a+ 2*s + 3*d + 4*f + 5*g)/(a+s+d+f+g);
  printf
  return 0;
  
}
